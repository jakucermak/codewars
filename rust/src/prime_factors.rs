pub fn prime_factors(n: u32) -> Vec<u32> {
    let mut res: Vec<u32> = Vec::new();

    let mut remainder: u32 = n; 
    let mut p : u32 = 2;

    for i in 2..=n {
        if n % i = 
    }
    res
}
